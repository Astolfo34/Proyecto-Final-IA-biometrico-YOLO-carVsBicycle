Eres un asistente experto en visión por computador. Genera un Jupyter Notebook (.ipynb) listo para ejecutar en Google Colab que implemente un proyecto académico sencillo de detección de objetos con YOLOv8, centrado en imágenes (con una celda opcional para video/webcam). El notebook debe trabajar con un dataset YA PREPARADO en formato YOLOv8 ubicado en la ruta relativa: `Z_ProyectoYolo/datasets` y producir resultados visuales y métricas básicas, sin pasos extra de empaquetado avanzado.

Objetivo (1–2 frases):
Entrenar y evaluar un modelo YOLOv8 que detecte y clasifique bicicletas vs autos en imágenes, midiendo precisión/recall y mAP@0.5, y mostrando inferencias sobre imágenes no vistas. Justificación breve por usabilidad (p. ej., conteo de tráfico, seguridad, estacionamientos inteligentes).

Requisitos del notebook (alineados al enunciado):
- Ejecutable en Colab sin interacción adicional salvo autorizar Drive (opcional).
- Instalar dependencias mínimas: `ultralytics`, `opencv-python-headless`, `matplotlib`, `pandas`.
- Variables al inicio: `PROJECT_ROOT = "Z_ProyectoYolo"`, `DATASET_ROOT = f"{PROJECT_ROOT}/datasets"`, `RANDOM_SEED`, `DEVICE` (auto), `OUTPUT_DIRS` básicos (`runs/`, `evaluation/`, `inference/`).
- Solo rutas relativas dentro de `Z_ProyectoYolo` (no rutas absolutas del sistema).
- Comentarios y textos en español, claros y breves.

Estructura mínima de carpetas usada/creada por el notebook:
Z_ProyectoYolo/
├─ datasets/ (YOLOv8)
│  ├─ images/train, images/val
│  └─ labels/train, labels/val
├─ runs/              # salida de entrenamiento (Ultralytics)
├─ evaluation/
│  ├─ metrics/        # metrics.json o métricas exportadas
│  └─ figures/        # gráficos de entrenamiento/métricas
├─ inference/
│  ├─ images_new/     # imágenes nuevas opcionales
│  └─ results_images/ # imágenes con predicciones
├─ modelo/            # best.pt (copia para entrega mínima)
└─ informe.md         # reporte breve (3–5 páginas)

Secciones del notebook (simplificadas y en este orden):
0) Setup
   - Instalar dependencias.
   - Definir variables: `PROJECT_ROOT`, `DATASET_ROOT`, `RANDOM_SEED`, `DEVICE`.
   - Detección de Colab y opción para montar Drive (comentado y fácilmente activable).
   - Mostrar versiones de `ultralytics` y `torch`.

1) Objetivo y justificación (breve)
   - 2–4 líneas con el objetivo y por qué es útil detectar bicicleta vs auto.

2) Verificación de dataset (estructura mínima YOLOv8)
   - Comprobar existencia de: `images/train`, `images/val`, `labels/train`, `labels/val`.
   - Verificar que cada imagen tenga su `.txt` correspondiente (listado corto de faltantes si aplica).
   - Contar imágenes por split y estimar conteo por clase a partir de los `.txt`.
   - Advertir si alguna clase tiene < 50 ejemplos (mensaje claro, sin bloquear ejecución).

3) Visualización rápida de datos
   - Mostrar 6–8 imágenes aleatorias con bounding boxes a partir de los `.txt`.

4) data.yaml
   - Usar `Z_ProyectoYolo/data.yaml` si existe; si no, crear uno con:
     train: Z_ProyectoYolo/datasets/images/train
     val:   Z_ProyectoYolo/datasets/images/val
     nc: 2
     names: ['bicicleta','auto']
   - Mostrar su contenido.

5) Selección de modelo
   - Variable `MODEL_BACKBONE = "yolov8n.pt"` (por compatibilidad con Colab gratuito). Breve nota de trade-off (velocidad vs precisión) y mención opcional a `yolov8s.pt`.

6) Entrenamiento (básico)
   - Ejecutar: `yolo detect train` con variables (`model`, `data`, `imgsz=640`, `epochs=50`, `batch=16`, `project=runs`, `name=exp_bici_auto`).
   - Nota breve de hiperparámetros y augmentaciones por defecto (mosaic, flips, hsv) sin entrar en detalle avanzado.
   - Registrar observaciones simples: pérdidas y tiempo por época (lectura de logs/JSON de Ultralytics).

7) Métricas y gráficos (básicos)
   - Extraer y mostrar: Precision, Recall, mAP@0.5.
   - Graficar pérdidas y, si es posible, P/R por época.
   - Guardar gráficos en `evaluation/figures/` y un `metrics.json` o CSV simple en `evaluation/metrics/`.

8) Inferencia (imágenes) y opcional video
   - Cargar `best.pt` y correr sobre `datasets/images/val` y `inference/images_new/` (si existen imágenes nuevas).
   - Guardar resultados visuales en `inference/results_images/` y mostrar 6–8 ejemplos.
   - Incluir una celda OPCIONAL (comentada) para inferencia en video o webcam en Colab.

9) Entrega mínima
   - Copiar `runs/*/weights/best.pt` a `modelo/best.pt`.
   - Asegurar que existan: `Z_ProyectoYolo/data.yaml`, `modelo/best.pt`, `inference/results_images/`.

10) Informe breve (3–5 páginas)
   - Generar `Z_ProyectoYolo/informe.md` con secciones:
     1) Descripción del problema y justificación.
     2) Proceso de creación del dataset (anotación: herramienta usada y breve explicación).
     3) Configuración de entrenamiento (modelo, hiperparámetros, augmentaciones).
     4) Resultados (métricas y 3–6 imágenes de inferencia guardadas).
     5) Conclusiones y mejoras propuestas (más datos, mejor anotación, cambiar modelo, más épocas, etc.).

Salidas esperadas (dentro de Z_ProyectoYolo/):
- `notebook.ipynb` (generado por el asistente)
- `data.yaml` (si no existía)
- `runs/.../weights/best.pt` y copia en `modelo/best.pt`
- `evaluation/metrics/metrics.json` (o CSV equivalente) y `evaluation/figures/*.png`
- `inference/results_images/*.png`
- `informe.md` (3–5 páginas)

Notas y buenas prácticas (acotadas):
- Mantener reproducibilidad con `RANDOM_SEED` y registrar versión de librerías.
- Mostrar advertencias, no detener el flujo, si hay desbalance o faltantes.
- Código y textos en español, con explicaciones cortas y prácticas.

Genera ahora el notebook `.ipynb` conforme a las secciones anteriores. No crees ZIPs ni checklists finales; céntrate en cumplir la entrega mínima solicitada por el enunciado.
